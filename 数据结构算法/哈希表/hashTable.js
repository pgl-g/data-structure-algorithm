/**
 * hash表实现
 * 
 * 采用链地址法来实现hash表
 */

/**
 * 原来不存在key，插入操作
 * 存在key，修改操作
 */

function hashTable(params) {
  // 属性
  this.storage = []; // 数组中存放相关的元素
  this.count = 0; // 当前已经存在了多少数据
  this.limit = 0; // 标记数组中一共可以存放多少个元素

  // 方法
}

